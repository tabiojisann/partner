version: 2.1 
jobs: 
  build: 
    docker:
      - image: node:10-browsers
    steps:
      - checkout
      - run:
          name: 最初のステップ
          command: |
            echo 'Hello World'
            echo 'This is a pen!'
      - run:
          name: コードの取得
          command: |
            ls -al
            echo '^^^That should look familiar^^^'
      - run:
          name: 独自コンテナで実行
          command: |
            node -v

